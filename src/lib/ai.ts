export type AIProvider={id:string;name:string;baseUrl:string;apiKey:string;model:string;enabled:boolean};export const defaultProviders:AIProvider[]=[{id:'openai',name:'OpenAI 兼容接口',baseUrl:'https://api.openai.com/v1',apiKey:'',model:'gpt-4o-mini',enabled:true},{id:'deepseek',name:'DeepSeek',baseUrl:'https://api.deepseek.com/v1',apiKey:'',model:'deepseek-chat',enabled:false},{id:'custom',name:'自定义模型',baseUrl:'',apiKey:'',model:'',enabled:false}];export async function chat(provider:AIProvider,prompt:string){if(!provider.baseUrl||!provider.apiKey)throw Error('请先配置 AI Provider');const r=await fetch(provider.baseUrl.replace(/\/$/,'')+'/chat/completions',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${provider.apiKey}`},body:JSON.stringify({model:provider.model,messages:[{role:'user',content:prompt}]})});if(!r.ok)throw Error(`AI 请求失败：${r.status}`);return(await r.json()).choices?.[0]?.message?.content||''}
